const fruits = ["Jabłko", "Banan", "Pomarańcza", "Winogrona", "Arbuz"];

const numbers = [26, 15, 32, 18, 0, 5, 1, 3, 4, 8];

for (let i = 0; i < numbers.length; i += 1) {
  if (numbers[i] > 5) {
    console.log(numbers[i]);
  }
}
